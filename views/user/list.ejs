<!DOCTYPE html>
<html>
<body>
<input type="button" onclick="javascrtpt:window.location.href='/user/add'" value="添加">
<table border="1">
    <tr>
        <th>账号</th>
        <th>昵称</th>
        <th>性别</th>
        <th>操作</th>
    </tr>
    <% for(var i = 0; i < list.length; i++) { %>
        <tr>
            <td><%= list[i].account %></td>
            <td><%= list[i].name %></td>
            <td>
                <%if(list[i].sex=="1" ){%> 男 <%}%>
                <%if(list[i].sex=="0" ){%> 女 <%}%>
            </td>
            <td><a href="/user/update?id=<%= list[i]._id %>">修改</a>
            <a href="/user/del?id=<%= list[i]._id %>">删除</a></td>
        </tr>
    <% } %>
</table>
<script>

</script>
</body>
</html>